## 1. Implementation

- [ ] 1.1 Add `patterns.phone()` generator for US phone format `(XXX) XXX-XXXX`
- [ ] 1.2 Add `patterns.phoneInternational()` generator for E.164 format `+1XXXXXXXXXX`
- [ ] 1.3 Add `patterns.creditCard()` generator with Luhn checksum validation
- [ ] 1.4 Add `patterns.isoDate()` generator for `YYYY-MM-DD` format
- [ ] 1.5 Add `patterns.isoDateTime()` generator for `YYYY-MM-DDTHH:mm:ssZ` format
- [ ] 1.6 Add `patterns.ssn()` generator for `XXX-XX-XXXX` format
- [ ] 1.7 Add `patterns.zipCode()` generator for US ZIP codes
- [ ] 1.8 Add `patterns.slug()` generator for URL-safe slugs

## 2. Testing

- [ ] 2.1 Add tests for `patterns.phone()` format validation
- [ ] 2.2 Add tests for `patterns.phoneInternational()` format validation
- [ ] 2.3 Add tests for `patterns.creditCard()` Luhn validation
- [ ] 2.4 Add tests for `patterns.isoDate()` format and validity
- [ ] 2.5 Add tests for `patterns.isoDateTime()` format and validity
- [ ] 2.6 Add tests for `patterns.ssn()` format validation
- [ ] 2.7 Add tests for `patterns.zipCode()` format validation
- [ ] 2.8 Add tests for `patterns.slug()` format validation

## 3. Documentation

- [ ] 3.1 Update docs/regex-design.md with new pattern examples
- [ ] 3.2 Ensure JSDoc comments are complete for all new patterns

## 4. Export

- [ ] 4.1 Verify all new patterns are exported from `src/index.ts`
